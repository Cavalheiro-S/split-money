export const JWT_TOKEN_COOKIE= "next-auth.session-token"

export const routes = {
    login: "/session/login",
    signup: "/session/signup",
    logout: "/session/logout",
    dashboard: "/dashboard",
    transaction: "/transaction",
}